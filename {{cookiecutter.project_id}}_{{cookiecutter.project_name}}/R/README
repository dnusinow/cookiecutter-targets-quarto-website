R files with the target functions are sourced from this directly automatically
by targets by default. You can also a include a list of targets in each file
that can be included in the list in the _targets.R file in the directory above.
